
# Tensorflow2-CPU-testing

```sh
# sudo apt install virtualenv
sudo apt install virtualenv
# set virtual environment
virtualenv --system-site-packages -p python3 ~/tensorflow-learn/venv/tf2
source ~/tensorflow-learn/venv/tf2/bin/activate
pip install tensorflow

```
